package es.santirivera.hastentest.fragment;

import es.santirivera.hastentest.base.interfaces.BaseNavigation;

public interface PlayerListListener extends BaseNavigation {

    // View doesn't currently respond to user input so the listener is left empty
}
